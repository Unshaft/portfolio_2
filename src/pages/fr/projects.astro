---
import { getCollection } from 'astro:content';
import ProjectCard from '../../components/ProjectCard.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';

const projects = (await getCollection('projects'))
  .filter((project) => project.data.locale === 'fr' && !!project.slug)
  .sort((a, b) => (a.data.order ?? 99) - (b.data.order ?? 99));


const lang = 'fr';



// Debug
console.log('📦 [FR] Projets affichés sur /fr/projects :');
projects.forEach((p) => {
  console.log(`— ${p.data.title} → /fr/projects/${p.slug}`);
});
---

<BaseLayout
  lang="fr"
  altLangHref="/en/projects"
  title="Projets - Killian Perzo"
  description="Découvrez mes projets en Data Analysis, Machine Learning, APIs et développement web. Budget Tracker, QueryBot API, DataBad et plus encore."
>
  <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-charcoal dark:text-white mb-6 sm:mb-8 px-2 sm:px-0">Mes projets</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6 px-2 sm:px-0">
    {projects.map((project) => (
      <a href={`/${lang}/projects/${project.slug}`} class="block group">
        <ProjectCard project={project.data} />
      </a>
    ))}
  </div>
</BaseLayout>
