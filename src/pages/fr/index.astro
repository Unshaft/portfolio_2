---
// src/pages/index.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allProjects = await getCollection('projects');
const projects = allProjects.filter(p => p.data.locale === 'fr');

// Debug console Astro
console.log('🧩 [index.astro] Projets FR visibles sur la homepage :');
projects.forEach(p => console.log(`— ${p.data.title} → /fr/projects/${p.slug}`));
---

<BaseLayout>
  <section class="text-center py-20">
    <h1 class="text-5xl font-bold text-charcoal mb-4">Bienvenue sur mon portfolio 👋</h1>
    <p class="text-lg text-gray-700 mb-8 max-w-xl mx-auto">
      Je suis Killian, analyste passionné de data & créateur de DataBad. Découvrez mes projets et mon parcours.
    </p>
    <a href="/fr/projects" class="bg-gold text-white px-6 py-3 rounded hover:bg-yellow-600 transition">
      Voir mes projets
    </a>
  </section>

  <section class="max-w-4xl mx-auto py-12">
    <h2 class="text-3xl font-bold mb-6">Projets récents</h2>
    <ul class="grid md:grid-cols-2 gap-6">
      {projects.slice(0, 2).map((project) => (
        <li class="p-4 border rounded hover:shadow transition">
          <a href={`/fr/projects/${project.slug}`}>
            <h3 class="text-xl font-semibold text-charcoal">{project.data.title}</h3>
            <p class="text-sm text-gray-600">{project.data.description}</p>
          </a>
        </li>
      ))}
    </ul>
  </section>
</BaseLayout>
