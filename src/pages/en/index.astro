---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allProjects = await getCollection('projects');
const projects = allProjects.filter(p => p.data.locale === 'en');

// Debug console Astro
console.log('🧩 [en/index.astro] English projects rendered on homepage:');
projects.forEach(p => console.log(`— ${p.data.title} → /en/projects/${p.slug}`));
---

<BaseLayout>
  <section class="text-center py-20">
    <h1 class="text-5xl font-bold text-charcoal mb-4">Welcome to my portfolio 👋</h1>
    <p class="text-lg text-gray-700 mb-8 max-w-xl mx-auto">
      I'm Killian, a data enthusiast and creator of DataBad. Discover my projects and journey.
    </p>
    <a href="/en/projects" class="bg-gold text-white px-6 py-3 rounded hover:bg-yellow-600 transition">
      View my projects
    </a>
  </section>

  <section class="max-w-4xl mx-auto py-12">
    <h2 class="text-3xl font-bold mb-6">Recent Projects</h2>
    <ul class="grid md:grid-cols-2 gap-6">
      {projects.slice(0, 2).map((project) => (
        <li class="p-4 border rounded hover:shadow transition">
          <a href={`/en/projects/${project.slug}`}>
            <h3 class="text-xl font-semibold text-charcoal">{project.data.title}</h3>
            <p class="text-sm text-gray-600">{project.data.description}</p>
          </a>
        </li>
      ))}
    </ul>
  </section>
</BaseLayout>
