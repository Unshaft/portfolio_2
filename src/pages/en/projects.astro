---
import { getCollection } from 'astro:content';
import ProjectCard from '../../components/ProjectCard.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';

const lang = 'en';

const projects = (await getCollection('projects')).filter(
  (project) => project.data.locale === lang && !!project.slug
);

console.log(`📦 [${lang.toUpperCase()}] Projects shown on /${lang}/projects:`);
projects.forEach((p) => {
  console.log(`— ${p.data.title} → /${lang}/projects/${p.slug}`);
});



---

<BaseLayout
  lang="en"
  altLangHref="/fr/projects"
  title="Projects - Killian Perzo"
  description="Explore my projects in Data Analysis, Machine Learning, APIs and web development. Budget Tracker, QueryBot API, DataBad and more."
>
  <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-charcoal dark:text-white mb-6 sm:mb-8 px-2 sm:px-0">My Projects</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6 px-2 sm:px-0">
    {projects.map((project) => (
      <a href={`/${lang}/projects/${project.slug}`} class="block group">
        <ProjectCard project={project.data} />
      </a>
    ))}
  </div>
</BaseLayout>
