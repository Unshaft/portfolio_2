---
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import '../styles/tailwind.css';
import SpeedInsights from "../components/SpeedInsights.astro";

interface Props {
  title?: string;
  description?: string;
  lang?: 'fr' | 'en';
  altLangHref?: string;
  ogImage?: string;
  scrollMode?: 'normal' | 'snap';
}

const {
  title = "Portfolio Killian Perzo",
  description = "Portfolio de Killian Perzo - Data Scientist spécialisé en IA Agentique. Découvrez mes projets en LangChain, LangGraph, systèmes multi-agents et automatisation intelligente.",
  lang = "fr",
  altLangHref,
  ogImage = "/img/og-image.png",
  scrollMode = "normal"
} = Astro.props;

const siteUrl = "https://killianperzo.dev";
const canonicalURL = new URL(Astro.url.pathname, siteUrl);

const isSnap = scrollMode === 'snap';
---

<html lang={lang} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="light dark" />
    <title>{title}</title>

    <!-- Prevent flash of wrong theme -->
    <script is:inline>
      if (localStorage.getItem('theme') === 'dark' || (!localStorage.getItem('theme') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      }
    </script>

    <!-- SEO Meta Tags -->
    <meta name="description" content={description} />
    <meta name="author" content="Killian Perzo" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={`${siteUrl}${ogImage}`} />
    <meta property="og:locale" content={lang === 'fr' ? 'fr_FR' : 'en_US'} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${siteUrl}${ogImage}`} />

    <!-- Alternate language -->
    {altLangHref && (
      <link rel="alternate" hreflang={lang === 'fr' ? 'en' : 'fr'} href={`${siteUrl}${altLangHref}`} />
    )}
    <link rel="alternate" hreflang={lang} href={canonicalURL} />

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png" />

    <!-- Preconnect for performance -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  </head>

  <body class:list={[
    "font-body bg-gray-50 dark:bg-midnight text-charcoal dark:text-gray-100 flex flex-col transition-colors duration-300 bg-grid-pattern",
    isSnap ? "h-screen overflow-hidden" : "min-h-screen"
  ]}>
    <Navbar lang={lang} altLangHref={altLangHref} />

    {isSnap ? (
      <main class="flex-1 flex justify-center items-center overflow-hidden">
        <div id="scroll-frame" class="w-full max-w-5xl h-full overflow-y-auto snap-y snap-mandatory scroll-smooth scrollbar-hide rounded-2xl border border-gray-200/50 dark:border-gray-700/30 bg-white/80 dark:bg-gray-900/80 backdrop-blur-sm shadow-2xl transition-colors duration-300">
          <slot />
        </div>
      </main>
    ) : (
      <main class="flex-1 w-full max-w-6xl mx-auto px-4 sm:px-6 py-16">
        <slot />
      </main>
    )}

    <Footer />
    <SpeedInsights />

    <script type="module">
      import Alpine from 'alpinejs';
      window.Alpine = Alpine;
      Alpine.start();
    </script>
  </body>
</html>
